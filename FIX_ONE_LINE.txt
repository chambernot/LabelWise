# ⚡⚡⚡ CORREÇÃO INSTANTÂNEA - UMA LINHA ⚡⚡⚡
# Copie e cole esta linha no PowerShell (raiz do projeto):

cd "C:\Users\chamb\source\repos\LabelWise\LabelWise.Api"; if (-not (Test-Path tessdata)) { mkdir tessdata }; Write-Host "📥 Baixando por.traineddata..." -ForegroundColor Cyan; Invoke-WebRequest -Uri "https://github.com/tesseract-ocr/tessdata/raw/main/por.traineddata" -OutFile "tessdata\por.traineddata" -UseBasicParsing; Write-Host "📥 Baixando eng.traineddata..." -ForegroundColor Cyan; Invoke-WebRequest -Uri "https://github.com/tesseract-ocr/tessdata/raw/main/eng.traineddata" -OutFile "tessdata\eng.traineddata" -UseBasicParsing; cd ..; dotnet clean --nologo -v q; dotnet build --nologo -v q; Write-Host "`n✅ PRONTO! Reinicie a API: dotnet run --project LabelWise.Api" -ForegroundColor Green

# O QUE ESTE COMANDO FAZ:
# 1. ✅ Cria diretório tessdata (se não existir)
# 2. ✅ Baixa por.traineddata (~4 MB)
# 3. ✅ Baixa eng.traineddata (~4 MB)
# 4. ✅ Limpa e recompila o projeto
# 5. ✅ Copia arquivos para bin/Debug/net10.0/tessdata

# DEPOIS: Reinicie a API
# dotnet run --project LabelWise.Api
