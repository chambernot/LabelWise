version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: labelwise_user
      POSTGRES_PASSWORD: changeme
      POSTGRES_DB: labelwise_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
